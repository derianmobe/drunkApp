import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  enableShadow: {
    shadowOffset: {
      width: 0,
      height: 1,
    },
    shadowOpacity: 0.25,
    shadowRadius: 1.84,
    elevation: 1,
  },
});

export default styles;
